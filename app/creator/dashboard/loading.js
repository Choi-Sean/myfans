export default function Loading() {
    return (
        <div style={{ padding: 20 }}>
            <div style={{ height: 120, background: "var(--card)", border: "1px solid var(--border)", borderRadius: 12, marginBottom: 12 }} />
            <div style={{ height: 280, background: "var(--card)", border: "1px solid var(--border)", borderRadius: 12 }} />
        </div>
    );
}
